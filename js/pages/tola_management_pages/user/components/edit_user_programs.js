import React from 'react'
import { observer } from "mobx-react"

export const EditUserPrograms = observer(
    ({onSave}) => {
        return <div className="edit-user-programs">
            test2
        </div>
    }
)

export default EditUserPrograms
